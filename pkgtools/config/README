
This directory contains GSA packaging configuration files.

esdlineup.list  nclMapping.list  packageMapping.list

esdlineup.list: (optional)
    This file contains the configuration for the GSA product ESD that is
    produced as part of its loadbuild.

nclMapping.list:
    This file defines the NCLs that are used for GSA packages. An NCL is an
    orderable set of software, which is used to make up a load.

packageMapping.list:
    This file provides the configuration for all packages that make up the GSA
    software load. This defines a package's attributes, such as role and the
    associated NCL.

